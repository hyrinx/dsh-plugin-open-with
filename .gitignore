# ── 依赖
node_modules/

# ── 包管理 & 本地缓存
package-lock.json

# ── 构建产物 / 生成代码
dist/
lib/
src/assets.ts
*.tsbuildinfo

# ── 本地运行日志（仅本地开发模式下由 logger 生成；最终用户模式日志仍在 ~/.dsh）
logs/
*.log

# ── npm 打包临时产物
*.tgz
.pkg_cache.json