# ── 依赖
node_modules/

# ── 包管理 & 本地缓存
package-lock.json

# ── 构建产物 / 生成代码
dsh-wallpaper-engine-main/
deepseek-harness-master/
src/assets.ts
*.tsbuildinfo

# ── 本地运行日志（仅本地开发模式下由 logger 生成；最终用户模式日志仍在 ~/.dsh）
logs/
*.log

# ── npm 打包临时产物
*.tgz
.pkg_cache.json

# ── IDE / 编辑器
.vscode/
.idea/
*.swp
*.swo

# ── OS 垃圾文件
.DS_Store
Thumbs.db
desktop.ini

# ── 本地环境变量
.env
.env.local